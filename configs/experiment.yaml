runner:
  tau: 300

smac:
  n_trials: 50  # Number of optimization trials (default: 100)
  seed: 42      # Random seed (default: 0)

rbfopt:
  max_evaluations: 50
  seed: 42

solvermind:
  max_edits: 5  # Number of parameters to change per trial (default: 3)
  n_trials: 20  # Number of optimization trials (default: 10)
  gpt_model: "gpt-4.1-mini"  # Model to use (default: "gpt-5-nano")

# logging:
#   out_dir: runs/rbfopt_test