data:
  instances_dir: instance/cvrp/mps
  pattern: "*.mps.gz"
  train_fraction: 0.3
  train_cap: 20
  use_explicit_split: false

runner:
  tau: 30

smac:
  n_trials: 5
  seed: 0

rbfopt:
  max_evaluations: 5
  seed: 0

logging:
  out_dir: runs/baselines_quick

